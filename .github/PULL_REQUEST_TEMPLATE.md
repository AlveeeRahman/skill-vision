## What does this PR change?

<!-- One or two sentences. Link the issue it addresses, if any. -->

## Checklist

- [ ] `python3 -m pytest tests -q` passes locally
- [ ] `python3 scripts/spec_validator.py .` still reports `CONFORMANT`
- [ ] No imports outside the Python standard library were added to `scripts/`
- [ ] New or changed checks come with tests (broken-skill fixture that proves the
      defect is caught)
